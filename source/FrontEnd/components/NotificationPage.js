import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';

export default function NotificationPage() {
  return (
    <View>
      <Text>This is NotificationPage</Text>
      <StatusBar style="auto" />
    </View>
  );
}
